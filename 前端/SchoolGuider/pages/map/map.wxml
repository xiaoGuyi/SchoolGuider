<!--pages/navigate/navigate.wxml-->
<view class='view'>
  <map longitude="{{longitude}}" latitude="{{latitude}}" markers="{{markers}}" covers="{{covers}}" style='width:{{windowWidth}}px;height:{{windowHeight}}px;' show-location>
  </map>
</view>